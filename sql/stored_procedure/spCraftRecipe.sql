CREATE PROCEDURE spCraftRecipe
    @recipeId INT,
    @itemId1 INT,
    @itemId2 INT,
    @itemId3 INT
AS BEGIN
    /*
        Check if recipe can be crafted

        Remove items

        Insert new item to database
    */
END;
GO